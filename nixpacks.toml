[phases.setup]
nixPkgs = ["...", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn wsgi:app --bind 0.0.0.0:${PORT:-5001} --workers 1 --timeout 120"

[variables]
PYTHONUNBUFFERED = "1"
