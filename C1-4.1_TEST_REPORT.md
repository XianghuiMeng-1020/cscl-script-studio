# C1-4.1 测试报告

## 测试执行摘要

**执行时间**: 2026-02-05

**测试范围**:
- `tests/test_cscl_decision_tracking_api.py` - 16个测试用例
- `tests/test_cscl_decision_summary_service.py` - 7个测试用例

**测试结果**:
```
27 passed, 0 failed in ~4.7s
```

**新增测试**:
- `tests/test_cscl_decision_export_snapshot.py` - 4个快照测试，验证导出格式稳定性

**修复内容**:
1. ✅ 修复`test_unauthenticated_create_decision_401` - 修改fixture避免session污染
2. ✅ 统一测试事务隔离策略 - `script_with_course` fixture改为直接创建，不依赖client
3. ✅ API契约一致性检查 - 创建`docs/API_ERROR_CODE_MATRIX.md`
4. ✅ 决策摘要指标稳定性 - 新增`decision_count_by_stage`和`decision_count_by_target_type`
5. ✅ 导出可复现实验元数据 - 增加`spec_hash`和`config_fingerprint`字段
6. ✅ 回归测试快照 - 新增快照测试确保导出格式稳定

**状态**: ✅ 全绿通过，可进入C1-5

**修复措施**:
1. 修改`script_with_course` fixture，使其不依赖`client` fixture，直接在app context中创建script
2. 在`test_unauthenticated_create_decision_401`中使用`use_cookies=False`创建test_client，确保session隔离

**验证命令**:
```bash
pytest -q tests/test_cscl_decision_tracking_api.py
pytest -q tests/test_cscl_decision_summary_service.py
pytest -q
```

**状态**: ✅ 全绿通过
